// Types for the correlation engine (renderer side)
// Re-export from shared location to ensure type consistency

export type {
  CorrelationResult,
  CorrelationMetadata,
  EnhancedCorrelationResult,
  CorrelationCalculationOptions
} from '@packages/db';
