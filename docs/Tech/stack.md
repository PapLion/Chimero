He actualizado tu **Matriz de Habilidades (The Chimero Stack 2.0)** para integrar estas nuevas potencias. Guárdala bien, porque esta es la estructura que usaremos para construir el resto de Chimero.

---

### 🧩 The Chimero Stack 2.0 (Matriz de Habilidades)

| Fase del Desarrollo | Skills a Activar | Objetivo y Rol |
| --- | --- | --- |
| **1. Ideación** | `brainstorming` | **El Product Manager.** Convierte ideas abstractas en requerimientos claros. |
| **2. Arquitectura & Diseño** | `writing-plans` + **`design-md`** + **`electron`** | **El Arquitecto.** Define la hoja de ruta técnica (`plans`), crea el Sistema de Diseño visual (`DESIGN.md`) y valida la arquitectura nativa (Main vs Renderer). |
| **3. Ingeniería Robusta** | `executing-plans` + `TDD` + **`typescript-expert`** | **El Senior Dev.** Escribe código seguro (type-safe), testeado y sin errores de lógica. |
| **4. Implementación UI** | **`tailwind-css-patterns`** + `frontend-design` | **El UI Specialist.** `frontend-design` da la visión estética (Cyberpunk), `tailwind-css-patterns` la implementa limpio y responsive. |
| **5. Optimización** | **`vercel-react-best-practices`** + **`typescript-advanced-types`** | **El Tech Lead.** Revisa el código para evitar re-renders innecesarios, cuellos de botella y usa tipos avanzados para blindar la app. |
| **6. Especialización de Datos** | `sql-expert` + `data-analysis` | **El Data Scientist.** Maneja la lógica compleja de SQL y matemáticas (correlaciones). |
| **7. Calidad & Debugging** | `systematic-debugging` + `webapp-testing` + `audit-website` | **El QA Engineer.** Pruebas E2E, auditoría de performance y resolución científica de bugs. |

---

### 🔄 El Nuevo Flujo de Trabajo (Prompt Template)

Fíjate cómo el prompt evoluciona. Ahora, antes de tocar código, definimos el **Sistema de Diseño** y validamos las **Best Practices**.

**Ejemplo de un Prompt Futuro (Sistema de Gamification):**

> **Task:** "Implementar sistema de Logros y Medallas"
> **Context:** Electron + React.
> **ACTIVATE SKILLS:**
> 1. **`brainstorming`**: Define los tipos de logros y la lógica de desbloqueo.
> 2. **`design-md`**: Analiza la UI actual y genera/actualiza `docs/DESIGN.md` para definir cómo se ven las medallas (colores, sombras, geometría) consistente con lo existente.
> 3. **`electron`**: Asegura que las notificaciones de logros usen la API nativa del sistema operativo.
> 4. **`writing-plans`**: Crea el plan paso a paso.
> 5. **`typescript-expert`**: Diseña interfaces estrictas (`Achievement`, `UserProgress`) usando tipos avanzados para evitar errores.
> 6. **`tailwind-css-patterns`**: Implementa las tarjetas de logros usando patrones móviles-first y utilidades optimizadas.
> 7. **`vercel-react-best-practices`**: Asegura que la lista de logros no cause re-renders masivos en el dashboard.
> 
> 

---

### 💡 Por qué esta actualización es CLAVE para Chimero

1. **`design-md`**: Es vital. Tu app tiene un estilo muy específico (Dark/Cyberpunk). Con esta skill, Windsurf leerá tu CSS actual y escribirá un "contrato de diseño" (`DESIGN.md`). Así, las nuevas features no se verán como "pegotes", sino 100% nativas.
2. **`electron`**: Antes Windsurf programaba "React genérico". Ahora sabe específicamente cómo manejar procesos de Electron (IPC, seguridad, ventanas), lo cual mejora la performance y la seguridad.
3. **`typescript-expert`**: Chimero crecerá. Sin tipos estrictos, se volverá inmanejable. Esta skill asegura que el código sea escalable y fácil de refactorizar en el futuro.

**¿Quieres que estrenemos el stack creando el archivo `DESIGN.md` para asegurar que todo lo futuro respete tu estética actual, o prefieres cerrar el tema de Stats primero?**