# Client Context Audit 03 - Assets, Calendar, Timeline

Scope: `Documentation/8_Client_Context.md` lines 1-939 only. Compared against `chimero-habit-flow/` and `Documentation/`.

| Requirement | Status | Code Evidence | Documentation Evidence | Gap | Recommended Next Action |
| --- | --- | --- | --- | --- | --- |
| Local image storage | DONE | `chimero-habit-flow/apps/electron/src/main/features/assets/service.ts:37` copies selected files into app `userData/assets`; `service.ts:73` returns stored path/thumbnail metadata; `handler.ts:31` inserts asset rows; `packages/db/src/schema.ts:88` defines local asset metadata. | `Documentation/8_Client_Context.md:119` says assets were stored locally; `Documentation/Contracts/Database_Contracts.md:40` documents files-on-disk with DB references. | Static code supports local storage. Manual upload/reload was not run. | Keep storage local-first and add a manual upload/reload check when verifying. |
| Clipboard paste asset input | MISSING | `chimero-habit-flow/apps/electron/src/renderer/src/features/assets/page.tsx:141` uploads only through `openFileDialog`; repo search for paste/clipboard handlers in `chimero-habit-flow/` returned no implementation hits. | `Documentation/8_Client_Context.md:48` says the client mostly uploaded through clipboard paste; `Documentation/8_Client_Context.md:121` lists Ctrl+V/clipboard paste as an input method; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:26` says true clipboard paste is not implemented. | No clipboard ingestion path found. | Add a paste handler that accepts clipboard image/file data and stores it through the asset service. |
| Drag/drop asset input | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/assets/page.tsx:123` handles drag over/leave; `page.tsx:135` handles drop; `page.tsx:138` routes drop to `handleUploadClick`; `page.tsx:252` says click or drop opens the file picker. | `Documentation/8_Client_Context.md:66` lists drag-drop support; `Documentation/8_Client_Context.md:121` lists drag and drop; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:26` says drag-drop routes to file picker rather than ingesting dropped files directly. | Drop interaction exists visually, but it does not read dropped files from `DataTransfer`; it opens the file picker. | Implement direct dropped-file ingestion and keep file-picker fallback for click. |
| File picker asset input | DONE | `chimero-habit-flow/apps/electron/src/main/features/assets/handler.ts:10` registers `open-file-dialog`; `handler.ts:11` opens native dialog; `handler.ts:19` uploads the selected source path; `AssetsPage.tsx:223` and `AssetsPage.tsx:244` trigger upload. | `Documentation/8_Client_Context.md:121` lists selecting files from folders; `Documentation/8_Client_Context.md:66` says click-to-upload was supported. | No static code gap found. | Preserve native file picker behavior while adding paste/drop ingestion. |
| Asset categories | BROKEN | `chimero-habit-flow/apps/electron/src/renderer/src/features/assets/page.tsx:25` defines product-ish categories such as People, Games, Books, TV; `page.tsx:117` filters by `asset.type`; `apps/electron/src/main/features/assets/service.ts:30` only infers `image` or `video`; `schema.ts:95` stores `type` as the backend type. | `Documentation/8_Client_Context.md:50` lists early categories; `Documentation/8_Client_Context.md:212` lists later categories; `Documentation/8_Client_Context.md:458` says assets did not clearly ask type/category. | Category buttons compare product categories against backend `image/video`, so uploaded assets cannot reliably belong to Games/Books/TV/Social categories through the Assets page. | Add a durable asset category field or category tags, expose category selection at upload/edit time, and migrate/filter against that field. |
| Asset preview | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/features/assets/page.tsx:51` previews an asset in the edit form; `page.tsx:327` renders grid thumbnails; `page.tsx:377` renders list thumbnails. | `Documentation/8_Client_Context.md:212` requires asset preview. | No static code gap found for library preview. | Keep preview behavior; verify broken/missing thumbnail fallback manually. |
| Asset search | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/features/assets/page.tsx:95` stores search query; `page.tsx:117` filters assets by name; `page.tsx:283` renders the search input. | `Documentation/8_Client_Context.md:66` and `Documentation/8_Client_Context.md:212` both list search support. | No static code gap found. | Preserve search and extend it to category/type fields once those are real. |
| Grid/list controls | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/features/assets/page.tsx:94` stores `grid`/`list` view mode; `page.tsx:292` renders the grid/list segmented buttons; `page.tsx:320` renders grid; `page.tsx:370` renders list. | `Documentation/8_Client_Context.md:66` and `Documentation/8_Client_Context.md:212` require grid/list controls. | No static code gap found. | Preserve both views during category persistence changes. |
| Asset assignment to entities | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/entry/components/QuickEntry.tsx:825` exposes entry asset attachment; `QuickEntry.tsx:879` selects an asset ID; `apps/electron/src/main/features/entry/handler.ts:49` stores `entries.assetId`; `packages/db/src/schema.ts:158` defines broader `asset_links`. | `Documentation/8_Client_Context.md:123` says assets support identity for tracked items and contacts; `Documentation/6_Chimero_Features.md:25` says assets are ACTIVE/PARTIAL and `asset_links` is broader than current single-asset flows. | Entries can attach a single selected asset, and contacts can upload avatars, but there is no general asset-linking UX for tracker entities/items and `asset_links` is not the primary active path. | Define entity assignment UX and use `asset_links` or a documented single-asset pattern consistently. |
| Social contact avatar assignment | PARTIAL | `chimero-habit-flow/packages/db/src/schema.ts:171` defines contacts; `schema.ts:174` stores `avatarAssetId`; `contacts/page.tsx:185` uploads a contact avatar; `contacts/page.tsx:199` updates an existing contact with `avatarAssetId`; `ContactBubblesGrid.tsx:169` checks `avatarAssetId` but falls back to initials. | `Documentation/8_Client_Context.md:184` requires contact images when present and initials otherwise; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:18` says avatar images are not shown in bubble UI yet. | Contact page can assign/upload an avatar, but social quick-entry bubbles still do not render the avatar image. | Render avatar thumbnails in `ContactBubblesGrid` using the same asset lookup pattern as `ContactProfilePage`. |
| Category/type selection | MISSING | `chimero-habit-flow/apps/electron/src/renderer/src/features/assets/page.tsx:141` upload has no category argument; `handler.ts:19` uploads only `sourcePath`; `service.ts:30` infers only `image`/`video`; edit form at `page.tsx:58` edits only name. | `Documentation/8_Client_Context.md:458` says assets did not clearly ask type/category; `Documentation/8_Client_Context.md:523` asks assets to ask category/type clearly. | No upload/edit control for choosing product category/type was found. | Add category/type selection to upload staging and edit modal; persist it separately from media MIME/type. |
| Calendar month grid | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/features/calendar/page.tsx:45` calculates first day; `page.tsx:46` calculates days in month; `page.tsx:356` renders the calendar panel; `page.tsx:371` renders the seven-column grid. | `Documentation/8_Client_Context.md:48` says the rough prototype had Calendar; `Documentation/8_Client_Context.md:516` asks Calendar to be functional and not just visual. | No static code gap found for month-grid existence. | Keep month grid behavior and verify layout at small widths. |
| Calendar category filters | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/calendar/page.tsx:27` tracks hidden calendar trackers; `page.tsx:165` toggles tracker visibility; `page.tsx:307` renders tracker pills for Calendar/Timeline. | `Documentation/8_Client_Context.md:48` says Calendar used category filters; `Documentation/8_Client_Context.md:66` says Calendar had category filters. | Current filters are tracker toggles based on trackers with entries, not stable product categories like TV/Exercise/Diet/Media/Tasks. | Decide whether filters should be tracker-based or product-category-based, then persist that model in the calendar contract. |
| Day/month/year review | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/calendar/page.tsx:150` navigates months; `page.tsx:425` renders selected-day detail; `page.tsx:240` renders year controls only for Timeline, not Calendar. | `Documentation/8_Client_Context.md:626` introduces Timeline under Calendar; `Documentation/8_Client_Context.md:646` references today/week/month/year analytics in tracker views. | Calendar supports month navigation and selected-day review; year review is effectively Timeline-only and not a calendar review mode. | Add explicit year review requirements to Calendar/Timeline contracts before changing UI. |
| Tracker data displayed in calendar | DONE | `chimero-habit-flow/apps/electron/src/main/features/calendar/service.ts:13` queries persisted entries; `service.ts:42` builds `CalendarDayEntry`; `calendar/page.tsx:95` maps tracker colors by day; `calendar/page.tsx:450` renders selected-day entries. | `Documentation/8_Client_Context.md:516` says Calendar must be functional; `Documentation/6_Chimero_Features.md:23` says Calendar/Timeline is ACTIVE/PARTIAL and Weight-enriched. | Real tracker data is present. Inline assets and resolved labels remain partial, but the core data display exists. | Keep status Done for data presence; audit richer selected-day rendering separately. |
| Calendar -> Timeline page/surface | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/features/calendar/page.tsx:20` tracks `calendar` vs `timeline`; `page.tsx:293` renders the Timeline tab; `page.tsx:653` renders `TimelineView`. | `Documentation/8_Client_Context.md:626` says the client requested a Timeline page under Calendar; `Documentation/8_Client_Context.md:628` names `Timeline`. | Implemented as a tab/surface under Calendar rather than a separate route, which appears consistent with the request. | Preserve Calendar -> Timeline navigation and decide later whether it needs its own route. |
| Timeline category toggles | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/calendar/page.tsx:28` tracks hidden timeline trackers; `page.tsx:177` toggles timeline tracker visibility; `TimelineView.tsx:190` renders tracker toggles in standalone mode. | `Documentation/8_Client_Context.md:629` says Timeline should allow selecting/deselecting categories similar to Calendar filters. | Toggles are tracker-based, not category-based. They also only appear externally in Calendar header when Timeline is embedded. | Align Calendar and Timeline on a shared category/filter model, or rename this as tracker toggles in docs. |
| Timeline months/eras visualization | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/calendar/components/TimelineView.tsx:47` groups entries by tracker and month; `TimelineView.tsx:258` renders a 12-month grid; `TimelineView.tsx:292` renders month cells with counts. | `Documentation/8_Client_Context.md:630` asks for months/eras visualization; `Documentation/8_Client_Context.md:635` describes horizontal time axis, event blocks, category colors, arrows, and legend. | Current Timeline is a year/month heatmap-count grid, not a horizontal era/event-block visualization with spans, arrows, or item periods. | Add period/span semantics and a true era visualization contract before implementing show/game ranges. |
| Timeline shows what months the user watched shows, played games, etc. | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/calendar/components/TimelineView.tsx:51` counts entries by timestamp month; `TimelineView.tsx:271` renders rows per tracker; `TimelineView.tsx:342` tooltip shows tracker/month/entry count. | `Documentation/8_Client_Context.md:630` asks to see what months the user watched a show or played a game; `Documentation/8_Client_Context.md:636` asks for tracker-derived periods and activity/media/game/show periods. | It can show that the Media/TV or Gaming tracker had entries in a month, but not which show/game item was active across months. | Model media/game/book item identity and date ranges, then render item-level month spans rather than tracker-level entry counts. |
