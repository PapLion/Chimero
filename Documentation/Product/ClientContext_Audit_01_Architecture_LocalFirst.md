# Client Context Audit 01: Architecture / Local-First Reality

Scope: `Documentation/8_Client_Context.md` lines 1-939 only, compared against current `chimero-habit-flow/` implementation and current `Documentation/` contract files. Irrelevant chat noise, payment, crypto, scheduling, greetings, and personal excuses were not audited.

Internal domain map used for this audit: Electron desktop target, Vite/Electron reality, TypeScript/JavaScript, Tailwind/styling, local-first storage, local database, privacy-first architecture, external dependency risk, IPC/preload/main/renderer boundaries, request/response contracts, DB contracts, and code/docs alignment.

| Requirement | Status | Code Evidence | Documentation Evidence | Gap | Recommended Next Action |
|---|---|---|---|---|---|
| Electron desktop app exists as the primary runtime. | DONE | `chimero-habit-flow/apps/electron/package.json:5-9` defines `electron-vite` dev/build/preview scripts; `chimero-habit-flow/apps/electron/src/main/index.ts:55-88` creates the main `BrowserWindow` and calls database setup before window creation; `chimero-habit-flow/apps/electron/src/main/index.ts:124` registers IPC handlers. | `Documentation/8_Client_Context.md:30-34` names Electron desktop first and local storage; `Documentation/8_Client_Context.md:380` says the client could run web/dev and eventually Electron; `Documentation/6_Chimero_Features.md:54-56` lists Electron main/preload/renderer paths. | Electron runtime exists. Linux/Wayland-specific behavior is outside this assigned audit except where lines 1-939 mention it as follow-up context. | Keep Electron as the primary architecture when resolving contract and persistence drift. |
| Vite/Electron current stack reality is active. | DONE | `chimero-habit-flow/apps/electron/package.json:6-8` uses `electron-vite`; `chimero-habit-flow/apps/electron/electron.vite.config.ts:3-4` imports `electron-vite` and React plugin; `chimero-habit-flow/package.json:19-22` builds/packages through the Electron app. | `Documentation/8_Client_Context.md:430` says the project moved from Next.js + Electron to Vite + Electron; `Documentation/8_Client_Context.md:512-513` requires active Vite + Electron and removal of old assumptions. | Current source uses Vite/Electron. | Continue treating old Next.js/Electron notes as historical unless current files prove otherwise. |
| TypeScript/JavaScript stack is present. | DONE | `chimero-habit-flow/apps/electron/tsconfig.json` exists; `chimero-habit-flow/apps/electron/src/main/index.ts`, `chimero-habit-flow/apps/electron/src/preload/index.ts`, and renderer `.tsx` files are TypeScript/TSX; `chimero-habit-flow/package.json:77` includes TypeScript. | `Documentation/8_Client_Context.md:32` names JavaScript/TypeScript as the preferred stack. | TypeScript is present across main, preload, renderer, shared contracts, and DB packages. | Keep cross-layer types in shared contracts rather than duplicating ad hoc shapes in renderer/main code. |
| Tailwind or current styling system exists. | DONE | `chimero-habit-flow/apps/electron/tailwind.config.js:1-63` configures Tailwind; `chimero-habit-flow/apps/electron/src/renderer/src/index.css:1-3` imports Tailwind layers; `chimero-habit-flow/apps/electron/src/renderer/src/index.css:7-8` defines dark theme tokens. | `Documentation/8_Client_Context.md:32` names Tailwind; `Documentation/8_Client_Context.md:327-330` lists dashboard/home, layout engine, and dark-mode direction. | Styling stack exists. | Keep styling evidence separate from feature-completeness claims; visual shell alone is not DONE for tracker behavior. |
| Local-first behavior is implemented. | PARTIAL | `chimero-habit-flow/apps/electron/src/main/database.ts:231-237` stores `chimero.db` under Electron `userData`; `chimero-habit-flow/packages/db/src/schema.ts:42-58` stores generic entries locally; `chimero-habit-flow/packages/db/src/schema.ts:130-141` adds specialized Weight tables; `chimero-habit-flow/apps/electron/src/main/features/tracking/service.ts:186-215` computes correlations from local entries. | `Documentation/8_Client_Context.md:33-34` requires local-first/local storage; `Documentation/8_Client_Context.md:119-127` extends this to assets, tracker data, and daily dashboard behavior; `Documentation/8_Client_Context.md:196-200` prefers local deterministic statistics over external AI; `Documentation/6_Chimero_Features.md:65-71` warns non-Weight trackers are still generic. | The app is local-first at runtime, but strict product local-first is only partial because many structured tracker requirements still collapse into generic local `entries` rather than tracker-specific queryable schemas. | Keep local-first storage, but do not mark structured tracker requirements DONE until each tracker has the needed schema/service/read model. |
| Local database exists and is wired. | PARTIAL | `chimero-habit-flow/packages/db/src/schema.ts:6-184` defines SQLite/Drizzle tables; `chimero-habit-flow/apps/electron/src/main/database.ts:231-280` initializes/migrates and seeds defaults; `chimero-habit-flow/apps/electron/src/main/features/index.ts:13-22` registers handlers that use DB-backed services. | `Documentation/8_Client_Context.md:32` asks for SQL/local DB; `Documentation/8_Client_Context.md:264-272` says local DB and granular structured data are high priority; `Documentation/Contracts/Database_Contracts.md:7` names local SQLite/Drizzle and `packages/db/src/schema.ts` as source of truth. | Local DB is real, but tracker usage is inconsistent in depth: Weight uses `entry_weight`, while most tracker families use generic `entries.value/note/metadata`. | Use Weight as the specialization template and add tracker-specific tables only when product contracts prove they are needed. |
| Privacy-first design is reflected. | PARTIAL | `chimero-habit-flow/apps/electron/src/main/features/tracking/service.ts:140-215` calculates stats/correlations locally; `chimero-habit-flow/apps/electron/src/main/features/tracking/handler.ts:273-301` exposes local stats/correlation IPC with fallback caveats; no external AI path appears in the audited code searches. | `Documentation/8_Client_Context.md:196-200` says private data should not be fed into external AI by default and should prefer local deterministic/statistical logic; `Documentation/8_Client_Context.md:222-224` repeats the local statistical correlation decision; `Documentation/Contracts/Backend_IPC_Contracts.md:61` documents local entries for stats/correlation. | Privacy-first analytics are reflected, but this is not a complete privacy architecture review and the exercise dataset fetch is still an external network dependency. | Keep AI/cloud out of baseline analytics; explicitly document any optional external dataset download as non-private tracker content and cache it locally. |
| No unnecessary external service dependency. | PARTIAL | Most runtime data paths are local IPC/SQLite, but `chimero-habit-flow/apps/electron/src/main/features/exercises/service.ts:2-11` documents/downloads `free-exercise-db` from GitHub and `chimero-habit-flow/apps/electron/src/main/features/exercises/service.ts:76-88` fetches it with `net.fetch`; web runtime falls back to same-origin `/api/*` in `chimero-habit-flow/apps/electron/src/renderer/src/shared/api.ts:5-37`. | `Documentation/8_Client_Context.md:34` says no cloud/VPS dependency unless a future technical need appears; `Documentation/8_Client_Context.md:196-198` rejects external AI by default; `Documentation/6_Chimero_Features.md:29` marks exercise search/cache partial. | The app does not appear cloud-dependent for private data, but it does depend on an external exercise dataset path for one support feature unless cached/available. | Classify external exercise DB download as optional/support data, add offline/cache behavior expectations, and avoid tying private tracker operation to network availability. |
| IPC/preload/main/renderer architecture exists. | DONE | `chimero-habit-flow/apps/electron/src/preload/index.ts:1-4` builds a typed `ElectronApi`; `chimero-habit-flow/apps/electron/src/preload/index.ts:5-83` maps `window.api` methods to `ipcRenderer.invoke`; `chimero-habit-flow/apps/electron/src/preload/index.ts:99` exposes `api`; `chimero-habit-flow/apps/electron/src/main/features/index.ts:13-22` registers feature handlers; renderer queries call API methods in `chimero-habit-flow/apps/electron/src/renderer/src/shared/queries.ts:41-523`. | `Documentation/8_Client_Context.md:655` and `682` say discovery must be converted into request/response flows and implementation boundaries; `Documentation/Contracts/Backend_IPC_Contracts.md:3` defines Electron IPC through preload/window.api; `Documentation/Flow/Contracts_Flow.md:7-14` documents renderer -> window.api -> preload -> handler -> service/DB -> shared response. | The architecture boundary is real. | Preserve this boundary; renderer should continue using query hooks/window.api, not SQLite directly. |
| Request/response contracts exist. | PARTIAL | `chimero-habit-flow/packages/shared/src/contracts/electron-api.ts:36-86` defines `ElectronApi`; `chimero-habit-flow/packages/shared/src/contracts/app-types.ts:34-67` defines generic entry request/response shapes; `chimero-habit-flow/packages/shared/src/contracts/app-types.ts:255-342` defines Weight request/detail contracts. | `Documentation/8_Client_Context.md:655`, `682`, `863`, and `935-936` require clean contracts, schemas, request/response flows, and read models before layering new requirements; `Documentation/Contracts/Shared_Contracts.md:45-53` lists active/partial shared contract families. | Contracts exist, but generic mutation methods still return `Entry | null`/booleans in places, and most non-Weight trackers lack specialized request/response/read models. | Normalize mutation responses and add specialized contracts only where current tracker product requirements need structured fields. |
| Database contracts exist and match code at the foundation level. | PARTIAL | `chimero-habit-flow/packages/db/src/schema.ts:6-184` defines current durable tables; `chimero-habit-flow/apps/electron/src/main/database.ts:61-80` inspects schema drift; `chimero-habit-flow/apps/electron/src/main/database.ts:231-280` runs migrations and seed logic. | `Documentation/Contracts/Database_Contracts.md:3-7` defines database ownership and SQLite/Drizzle source of truth; `Documentation/Contracts/Database_Contracts.md:112-114` warns schema changes affect local user data; `Documentation/6_Chimero_Features.md:31-34` states Weight is specialized and non-Weight specialization is not present. | Foundation DB docs match code, but product-level DB contracts for many structured trackers are still not implemented. | Keep DB contract docs honest: generic `entries` is PARTIAL for structured trackers, not a substitute for tracker-specific schemas. |
| Code/docs alignment is maintained for the audited architecture area. | PARTIAL | Current source paths align with docs for Electron main/preload/renderer, shared contracts, DB schema, Weight specialization, generic entries, Calendar, Assets, and Quick Entry. Evidence includes `chimero-habit-flow/apps/electron/src/renderer/src/App.tsx:23-37`, `chimero-habit-flow/apps/electron/src/preload/index.ts:5-83`, `chimero-habit-flow/packages/db/src/schema.ts:42-58`, and `chimero-habit-flow/apps/electron/src/main/features/weight/service.ts:65-106`. | `Documentation/6_Chimero_Features.md:3` says filesystem/shared contracts win over older planning notes; `Documentation/6_Chimero_Features.md:65-71` lists current implementation gaps; `Documentation/8_Client_Context.md:483-495` warns visual presence is not completion; `Documentation/8_Client_Context.md:935-936` requires contracts/read models before new requirements. | Current docs are mostly honest, but `8_Client_Context.md` lines 1-939 include historical status statements that can sound complete unless cross-checked against current contract docs and source. | Treat `8_Client_Context.md` as product history/context and keep `6_Chimero_Features.md`, contract files, and source as the implementation reality check. |
