# Client Context Audit 07: Stats and Correlations

Scope: read-only audit of `Documentation/8_Client_Context.md` lines 1-939 against current `chimero-habit-flow/` and `Documentation/`. Status vocabulary is restricted to: DONE, PARTIAL, MISSING, BROKEN, DOC_ONLY, CODE_ONLY, UNCLEAR.

| Requirement | Status | Code Evidence | Documentation Evidence | Gap | Recommended Next Action |
|---|---|---|---|---|---|
| Global Stats page | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/App.tsx:12`, `:31-32` routes `stats` to `StatsPage`; `chimero-habit-flow/apps/electron/src/renderer/src/features/tracking/page.tsx:14-35`, `:57-128` renders Insight Lab correlation UI. | `Documentation/8_Client_Context.md:80`, `:170-174`, `:360` request a global Stats / Advanced Statistics page for historical facts. `Documentation/6_Chimero_Features.md:24` calls Insight Lab / Stats ACTIVE/PARTIAL. | Page exists, but it is mainly a pairwise correlation/hypothesis page, not the broad arbitrary historical Stats page requested. | Keep the current local Insight Lab, but design a separate global Stats query surface for historical facts and saved metrics. |
| Tracker-specific Statistics tabs | PARTIAL | `TrackerDetailView.tsx:131` defines `stats`; `:429-441` renders Statistics tab; `:486-589` renders generic cards plus Weight-specific read model values. | `Documentation/8_Client_Context.md:638-649` requires Statistics tabs as mini-app surfaces. `Documentation/Contracts/Frontend_Contracts.md:66` says non-Weight statistics are mostly generic and should not be overclaimed. | Statistics tab exists, but most trackers use generic entry counts/averages instead of tracker-specific read models. | Promote tracker-specific statistics only after each tracker contract has backend/shared read models. |
| Tracker-specific Graphs tabs | PARTIAL | `TrackerDetailView.tsx:131-133` defines graph state; `:601-752` renders charts from entry/Weight chart data; `:756-790` renders heatmap from entries. | `Documentation/8_Client_Context.md:641-647` separates Graphs from Statistics and requests line charts, filters, and heatmaps. `Documentation/Contracts/Frontend_Contracts.md:67` allows generic entry-derived series but notes gaps. | Graphs use real data, not mock arrays, but they are generic `value`/density graphs for most trackers and do not cover tracker-specific axes. | For each tracker, define chartable fields and empty-state rules in the tracker contract before expanding graphs. |
| Entries tabs | PARTIAL | `TrackerDetailView.tsx:813-1175` renders Entries with edit/delete affordances, tags, and assets; `entry/handler.ts:15-34`, `:36-63`, `:65-107` provides generic CRUD. | `Documentation/8_Client_Context.md:286`, `:575-578`, `:649` require CRUD and Entries tabs. `Documentation/Contracts/Frontend_Contracts.md:65` requires exact logged history and warns not to invent fields. | Entries exist, but generic trackers expose only the generic fields captured, so structured tracker histories remain incomplete. | Keep generic Entries as baseline and add specialized entries read models where tracker contracts require richer fields. |
| Retroactive queries | PARTIAL | `tracking/service.ts:85-136` filters stats by tracker IDs, tag IDs, date range, and day/week/month buckets; `app-types.ts:193-199` only exposes these query fields. | `Documentation/8_Client_Context.md:172-174`, `:265-278` require retroactive SQL-friendly historical questions. | Current query contract supports basic filters, not arbitrary "last time I ate tacos" or structured field queries. | Add a Stats query model for saved historical facts, tag inheritance, and tracker-specific field predicates. |
| "Last time" queries | PARTIAL | `TrackerDetailView.tsx:210-215` calculates last entry date; `:581-588` displays Days Since Last Entry/Meal/Task. | `Documentation/8_Client_Context.md:80`, `:172` explicitly asks for last time alcohol/fast food/tacos. | Only generic last entry per tracker is visible; no last-time query by food/tag/activity/event. | Add last-occurrence queries over tags and tracker-specific fields, with a UI for selecting the subject. |
| "How many" queries | PARTIAL | `tracking/service.ts:122-135` returns entry counts and series counts; `TrackerDetailView.tsx:249-250`, `:542-557` counts entries this week/year. | `Documentation/8_Client_Context.md:80`, `:172` asks how many times and totals such as eggs weekly/yearly. | Counts exist for entries, not for arbitrary named foods/items or structured quantities. | Implement count/sum metrics over explicit tags and structured tracker fields. |
| Days since metrics | PARTIAL | `TrackerDetailView.tsx:192-215` computes days since last entry; `:581-588` displays it. | `Documentation/8_Client_Context.md:172`, `:565-568`, `:572` request days-since metrics. | Days-since is implemented only as last entry for the current tracker, not configurable days-since for a selected item/tag/activity. | Extend Stats to support saved days-since cards for tags, assets, and structured tracker entities. |
| Daily/weekly/monthly/3-month/yearly windows | PARTIAL | `app-types.ts:193-199` supports stats `groupBy` day/week/month only; `TrackerDetailView.tsx:262-299` supports graph windows `1M`, `3M`, `1Y`; `:612-626` renders those window buttons. | `Documentation/8_Client_Context.md:646-647` requests today/week/month/year and daily/weekly/monthly/yearly filters. | The implementation has some windows, but not one coherent stats/graphs window system including daily, weekly, monthly, 3-month, and yearly across surfaces. | Normalize time-window contracts across global Stats, tracker Statistics, and tracker Graphs. |
| Local statistical correlations | PARTIAL | `tracking/service.ts:139-158` returns local correlation caveats; `:164-318` calculates local pairwise impact from entries; `tracking/page.tsx:72-110` displays results. | `Documentation/8_Client_Context.md:194-200`, `:220-224` requires local deterministic/statistical correlations. `Documentation/Contracts/Shared_Contracts.md:53` marks stats/correlation ACTIVE/PARTIAL. | Real local algorithm exists, but it is limited to source tracker vs target tracker with offset and scalar values. | Keep the local engine, then add structured source selectors for tags, assets, and tracker-specific events. |
| No external AI by default | DONE | `tracking/service.ts:189-318` uses local DB entries and local calculation; `rg "openai|anthropic|gemini|llm|external AI|AI" chimero-habit-flow/apps chimero-habit-flow/packages -S` returned no app/package hits. | `Documentation/8_Client_Context.md:194-200`, `:220-224` says external AI must not be the baseline. | No app/package evidence of external AI in the audited paths. | Preserve the local-first default; treat any AI as an explicit opt-in future feature. |
| Low mood after alcohol correlation | PARTIAL | `HypothesisBuilder.tsx:48-98` lets user select two trackers and same/next-day lag; `tracking/service.ts:195-224` maps source/target entries by date. | `Documentation/8_Client_Context.md:103-107`, `:131-133` cite low mood after drinking. | Possible only if alcohol is represented as a tracker or numeric source; no alcohol tag/item query is built into the correlation UI. | Add tag/item-based source cohorts for Diet/Social/Mood examples. |
| Happy mood after social contact correlation | PARTIAL | Same pairwise engine: `tracking/service.ts:237-318`; Social default tracker exists in `tracking/handler.ts:14`. | `Documentation/8_Client_Context.md:103-107`, `:131-133`, `:778-785` cite social contact and aggregate social charts. | Pairwise Social-to-Mood can run, but contact-level and interaction-quality fields are not part of the generic correlation selector. | Connect Social contact interactions/read models to the correlation source model. |
| Food tags with health symptoms correlation | MISSING | Tags exist in `schema.ts:102-128` and stats can filter explicit tags via `tracking/service.ts:90-105`, but no Health tracker/default symptom model or food-to-health correlation UI was found in audited code. | `Documentation/8_Client_Context.md:755-762`, `:812-826`, `:889-896` require food tags with Health symptom correlations. | Tag infrastructure exists, but the requested Health symptom correlation path is absent. | Define Health tracker schema, symptom tags, and tag-inheritance-aware food/symptom correlation queries before UI work. |
| Game losses affecting mood correlation | MISSING | Gaming default is text/generic in `tracking/handler.ts:18`; generic entries store `value`/`note`/`metadata` in `schema.ts:42-55`; no win/loss enum/query path found. | `Documentation/8_Client_Context.md:748-753` requests wins/losses and mood impact. | No structured win/loss data exists for Gaming correlations. | Add Gaming session schema with win/loss fields and mood-correlation read model. |
| Exercise/sleep/mood correlations | PARTIAL | Exercise default tracker exists in `tracking/handler.ts:13`; Mood exists in `:12`; generic pairwise correlation can compare tracker values via `tracking/service.ts:164-318`. | `Documentation/8_Client_Context.md:719-726`, `:728-738`, `:796` discuss sleep/exercise/mood/health correlations. | Mood and Exercise can be compared generically, but Sleep is not a default structured tracker in current evidence and exercise data is not a structured workout correlation model. | Add Sleep contract/implementation and Exercise structured fields before claiming domain correlation support. |
| Diet/weight correlations | PARTIAL | Diet default exists in `tracking/handler.ts:20`; Weight has specialized read models in `TrackerDetailView.tsx:146-153`; generic correlation can compare trackers via `tracking/service.ts:164-318`. | `Documentation/8_Client_Context.md:708`, `:755-762`, `:873-896` require food tags and Weight/Health correlations. | Diet-to-Weight can be generic numeric, but ingredient/tag/meal semantics and inherited food tags are not first-class in the correlation engine. | Build Diet tag/ingredient schema and connect it to Weight correlation sources. |
