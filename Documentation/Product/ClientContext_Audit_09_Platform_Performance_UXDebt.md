# Client Context Audit 09: Platform, Performance, and UX Debt

Scope: read-only audit of `Documentation/8_Client_Context.md` lines 1-939 against current `chimero-habit-flow/` and `Documentation/`. Status vocabulary is restricted to: DONE, PARTIAL, MISSING, BROKEN, DOC_ONLY, CODE_ONLY, UNCLEAR.

| Requirement | Status | Code Evidence | Documentation Evidence | Gap | Recommended Next Action |
|---|---|---|---|---|---|
| Electron app startup | PARTIAL | `apps/electron/src/main/index.ts:79-143` initializes DB, exercise DB, IPC, windows, reminder loop, and splash/main show behavior; `apps/electron/src/main/database.ts:231-280` initializes DB and migrations. | `Documentation/Flow/Backend_Flow.md:3-7` documents startup responsibilities; `Documentation/8_Client_Context.md:401`, `:508` says Electron run status must be checked. | Startup code exists, but this audit did not run Electron or verify startup on target machines. | Run current Electron startup smoke tests on the supported platforms before marking DONE. |
| Linux/Arch compatibility | UNCLEAR | Root `package.json:47-54` only defines Windows NSIS packaging; `apps/electron/src/main/index.ts:21-25` uses Windows-first env vars with fallback; `apps/electron/src/main/database.ts:232-239` logs absolute DB/migration paths in dev. | `Documentation/8_Client_Context.md:436-449`, `:508` requires Linux/Arch testing. | No current Linux/Arch test evidence or Linux build target was found in inspected code/docs. | Add Linux packaging/test instructions and run Arch/Linux startup verification. |
| Wayland behavior | UNCLEAR | `BrowserWindow` uses `titleBarStyle: 'hidden'` in `apps/electron/src/main/index.ts:55-68`; drag regions are CSS-only in `index.css:221-227`. | `Documentation/8_Client_Context.md:438-449`, `:456-461`, `:509` calls out Arch/Wayland and window dragging. | Wayland behavior is platform-specific and untested in this audit. | Verify under Wayland and document any Electron flags or titlebar fallback needed. |
| Absolute path issue | PARTIAL | `apps/electron/src/main/index.ts:21-29`, `:100-121` uses `resolve`, `join`, `normalize`, `pathToFileURL`; `apps/electron/src/main/database.ts:22-44`, `:231-239` resolves migrations and DB paths and logs `isAbsolute`. | `Documentation/8_Client_Context.md:439-449`, `:511` requires absolute path safety. | Current code uses absolute path helpers in critical startup paths, but no Linux/Mac runtime proof is present. | Keep absolute-path checks and verify packaged Linux/Mac builds, not only dev mode. |
| Window/titlebar dragging | PARTIAL | `TitleBar.tsx:8-12` applies `drag-region`; `index.css:221-227` maps `.drag-region` to `-webkit-app-region: drag` and excludes buttons/inputs; `main/index.ts:66` hides native titlebar. | `Documentation/8_Client_Context.md:456-461`, `:510` requires movable titlebar/window. | Drag region exists, but custom titlebar controls are hidden and Wayland behavior is not proven. | Test drag on Windows/Linux Wayland/X11 and add fallback/titlebar policy if needed. |
| Custom window management | PARTIAL | `main/index.ts:35-52` creates splash window; `:55-76` creates main window; `TitleBar.tsx:25-35` contains hidden minimize/maximize/close buttons without IPC handlers. | `Documentation/8_Client_Context.md:449`, `:456`, `:550-552` requires robust window/titlebar behavior. | Window creation exists, but custom controls are not functional UI and cross-platform window management is incomplete. | Either expose real window-control IPC or rely on native controls per platform. |
| Vite + Electron migration reality | DONE | `apps/electron/package.json:5-9`, `:45-50` uses `electron-vite` and Vite; `apps/web/package.json:6-10`, `:37-43` uses Vite; root `package.json:19-22`, `:72-78` uses Vite/React. | `Documentation/8_Client_Context.md:428-434`, `:512` says the Vite + Electron claim must be audited. `Documentation/Flow/App_Flow.md:11-15` documents Electron/Web Vite mounting. | Current package/runtime evidence supports Vite + Electron. | Keep migration docs aligned with package scripts and avoid old Next.js assumptions. |
| Old Next.js assumptions | DONE | `rg -n "next|Next\\.js|@next" chimero-habit-flow/package.json chimero-habit-flow/apps chimero-habit-flow/packages -S` returned no Next.js framework dependency/reference hits in source/package paths. | `Documentation/8_Client_Context.md:430-433`, `:513` requires removal of old Next.js/Electron assumptions. | No current source/package evidence of Next.js assumptions in audited paths. | Keep docs/spec language Vite/Electron/Web-adapter based. |
| Overflow/responsive issues | PARTIAL | `App.tsx:43-55` uses fixed viewport shell with nested overflow; `TrackerDetailView.tsx:398`, `:480`, `:757-762` relies on scroll/overflow containers; `index.css:57-75` globally hides body overflow. | `Documentation/8_Client_Context.md:50`, `:403`, `:457`, `:518` requires avoiding overflow and hidden controls. | Many containers are scroll-aware, but nested fixed-height/overflow layouts can still hide content; no responsive visual verification was run. | Run viewport QA for Home, Tracker Detail, modals, Assets, Calendar, and Custom Trackers. |
| Custom tracker modal overflow | PARTIAL | `CreateTrackerDialog.tsx:248-249` sets dialog content overflow hidden and inner `max-h-[85vh] overflow-y-auto`; form is long at `:273-534`. | `Documentation/8_Client_Context.md:457`, `:465-479`, `:518` specifically calls out custom tracker modal overflow. | The current modal has a scroll container, likely improved from the reported issue, but not visually tested here and still uses a long vertical form. | Browser/Electron screenshot test the modal at small desktop heights and tighten layout if controls are hidden. |
| Asset modal UX | PARTIAL | `AssetsPage.tsx:25-33` includes People/Social category; `:35-83` edit asset form changes only name; `:123-139`, `:232-259` drop opens file picker instead of direct file ingest. | `Documentation/8_Client_Context.md:453-460` reports asset category/type UX and social asset issues. | Asset category labels improved, but upload/edit flow does not clearly assign category/type beyond inferred asset type and drop behavior is shallow. | Add explicit asset category/type assignment and true drop/paste ingestion if product still wants it. |
| 500ms lag/performance issue | UNCLEAR | Some memoization/abort work exists in `tracking/page.tsx:37-55`, `:132-134` and `useCorrelationCalculation.ts:51-66`, but no measurement was found. | `Documentation/8_Client_Context.md:404-412`, `:515` requires proving 500ms lag is gone. | Performance was not measured in this audit. | Add a repeatable performance check for tracker switching, quick entry open, and correlation runs. |
| Tracker switching performance | UNCLEAR | `BentoGrid.tsx:68-85`, `:130-136` swaps to `TrackerDetailView`; `TrackerDetailView.tsx:109-159`, `:397-407` loads all entries and animates by tracker ID. | `Documentation/8_Client_Context.md:405`, `:412`, `:514` calls tracker switching sluggishness a requirement. | Code has memoization but still reads broad entry sets; no timing/profile evidence exists. | Profile tracker switching with realistic entry counts before claiming fixed. |
| Production polish risks | PARTIAL | `Documentation/6_Chimero_Features.md:20-34` marks many core features ACTIVE/PARTIAL; `Documentation/Contracts/Frontend_Contracts.md:62-69` warns non-Weight/generic surfaces must not overclaim. | `Documentation/8_Client_Context.md:401-426`, `:481-495`, `:497-518` says visual presence must be separated from production readiness. | The app has many real paths, but custom trackers, generic analytics, platform testing, and performance remain not production-proven. | Maintain a production readiness checklist with platform, performance, data-model, and visual QA gates. |
