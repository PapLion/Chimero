# Client Context Audit 08: Custom Trackers and Technical Debt

Scope: read-only audit of `Documentation/8_Client_Context.md` lines 1-939 against current `chimero-habit-flow/` and `Documentation/`. Status vocabulary is restricted to: DONE, PARTIAL, MISSING, BROKEN, DOC_ONLY, CODE_ONLY, UNCLEAR.

| Requirement | Status | Code Evidence | Documentation Evidence | Gap | Recommended Next Action |
|---|---|---|---|---|---|
| Custom tracker creation | PARTIAL | `CustomTrackersPage.tsx:104-110`, `:215-238` opens `CreateTrackerDialog`; `CreateTrackerDialog.tsx:188-225` submits create/update; `tracking/handler.ts:67-93` persists a tracker row. | `Documentation/8_Client_Context.md:82-86`, `:467-479` require custom tracker creation but warn existence is not production readiness. `Documentation/6_Chimero_Features.md:26` marks custom trackers ACTIVE with generic entry behavior. | Creation works for simple tracker definitions, but not for meaningful schema/widget/analytics systems. | Keep simple creation as an MVP path, then redesign the domain model before adding more custom-tracker features. |
| Tracker name/icon/color | DONE | `CreateTrackerDialog.tsx:151-155`, `:322-392` edits name/icon/color; `tracking/handler.ts:78-83`, `:224-238` stores and updates them. | `Documentation/8_Client_Context.md:467-471` lists these modal fields. | This visual identity layer is implemented. | No immediate action beyond preserving validation and UI polish. |
| Tracker type: counter/rating/list/etc. | PARTIAL | `CreateTrackerDialog.tsx:139-144`, `:395-512` supports Counter/Rating/List; `tracking/handler.ts:73-83`, `:239-243` maps UI types to numeric/range/text. | `Documentation/8_Client_Context.md:472-476` lists Counter/Rating/List; `:149-160` requires real field types and schemas. | Type selection is shallow: it maps to one generic schema type and a small config object, not a robust custom tracker type system. | Define supported tracker archetypes and how each maps to persistence, validation, UI, stats, graphs, and correlations. |
| Schema builder | MISSING | `TrackerConfig` only exposes `min`, `max`, `unit`, `goal`, `step`, `options`, `isCustom` in `app-types.ts:10-18`; `CreateTrackerDialog.tsx:443-512` only edits unit/goal/min/max/options. | `Documentation/8_Client_Context.md:149-160`, `:477-479` requires a real schema builder/configurable field system. | No evidence of a field schema builder, per-field validation, field IDs, field types, or custom read models. | Design a custom schema format before adding additional UI controls. |
| Configurable fields | PARTIAL | `schema.ts:28-30` stores tracker `config`; `schema.ts:53-55` stores entry `metadata`; `BaseEntryRequest` accepts metadata in `app-types.ts:46-54`. | `Documentation/8_Client_Context.md:84`, `:149-160` requires configurable fields such as image, description, title, counter, time, and structured data. | Storage can hold JSON, but the UI only configures a few scalar options and does not generate field-specific entry forms or analytics. | Add field definitions with stable IDs, validation rules, renderer controls, and migration/version behavior. |
| Units | DONE | `CreateTrackerDialog.tsx:443-469` captures counter unit; `TrackerConfig.unit` exists in `app-types.ts:10-14`; tracker cards display unit in `CustomTrackersPage.tsx:196-200`. | `Documentation/8_Client_Context.md:475` lists unit. | Units exist for simple counter-style trackers. | Keep units, but bind them to field schemas when custom fields exist. |
| Daily goals | PARTIAL | `CreateTrackerDialog.tsx:457-468` captures a daily goal; `TrackerConfig.goal` exists in `app-types.ts:10-15`; `CustomTrackersPage.tsx:201-205` displays goal. | `Documentation/8_Client_Context.md:476` lists daily goal. | Goal is stored/displayed but not proven as a full analytics/notification/progress contract for custom trackers. | Define how daily goals affect widgets, stats, streaks, calendar, and reminders. |
| Custom tracker data persistence | PARTIAL | Tracker definitions persist in `trackers` (`schema.ts:18-38`); entries persist generically in `entries` (`schema.ts:42-70`); generic entry writes are `entry/handler.ts:36-63`. | `Documentation/8_Client_Context.md:481-495` says persistence and schema quality must be evaluated separately. `Documentation/Contracts/Database_Contracts.md:37-38` says trackers define what can be logged; entries are generic and semantics depend on tracker. | Data persists, but mostly as `value`/`note`/`metadata`, which is not enough for robust custom schemas. | Separate custom tracker definition persistence from custom entry field persistence/read models. |
| Custom tracker bento widget | PARTIAL | `BentoGrid.tsx:28-65`, `:80-85`, `:139-158` adds widgets for all trackers, including custom trackers; `WidgetCard` receives tracker and entries. | `Documentation/8_Client_Context.md:82-84`, `:853` requires bento widget behavior; `Documentation/6_Chimero_Features.md:20`, `:26` says dashboard is active but generic widgets use frontend heuristics. | Custom trackers appear as widgets, but widget behavior is generic and not schema-aware. | Add widget templates derived from custom schema or explicit widget config. |
| Custom tracker stats/graphs/entries | PARTIAL | `CustomTrackersSection.tsx:67-79` selects a custom tracker into `TrackerDetailView`; `TrackerDetailView.tsx:429-465`, `:486-811`, `:813-1175` provides generic Stats/Graphs/Entries. | `Documentation/8_Client_Context.md:641-649`, `:854-857` require tabs/statistics/graphs/entries. `Documentation/Contracts/Frontend_Contracts.md:65-67` says generic surfaces must not be overclaimed. | Custom trackers inherit generic detail tabs, but analytics are based on generic values and cannot understand custom fields. | Build schema-aware statistics and graph adapters for custom tracker field types. |
| Custom tracker relation to correlation engine | PARTIAL | Custom tracker rows can be selected in `HypothesisBuilder.tsx:52-96` because it uses all trackers; correlation uses generic entries in `tracking/service.ts:195-224`. | `Documentation/8_Client_Context.md:149-162`, `:861` requires analytics/correlation readiness. | Custom trackers can participate only as scalar/generic source-target trackers, not as field/tag/entity-aware correlation subjects. | Add correlation source definitions for custom fields and tags. |
| Custom tracker edit/delete | DONE | `CustomTrackersPage.tsx:44-62`, `:161-180`, `:233-249` supports edit/delete UI; `tracking/handler.ts:95-102`, `:220-252` handles delete/update. | `Documentation/8_Client_Context.md:858` requires edit/delete behavior. | Edit/delete exists for tracker definitions; deleting cascades entries through DB foreign key by tracker ID. | Preserve current behavior but add delete/archival policy before production data grows. |
| Boundary between custom trackers and specialized default trackers | PARTIAL | `tracking/handler.ts:10-21` seeds default trackers with `isCustom: false`; `CreateTrackerDialog.tsx:193-202` creates custom tracker data; `schema.ts:32-36` has `isCustom`. Weight is specialized separately in `Documentation/6_Chimero_Features.md:34`. | `Documentation/8_Client_Context.md:86`, `:147-153`, `:161` requires clear separation. | The `isCustom` flag exists, but many default trackers still behave generically, so the boundary is not yet architectural enough. | Define a tracker capability matrix: generic custom, default generic, and specialized default. |
| Current technical debt | PARTIAL | `app-types.ts:10-18`, `schema.ts:42-55`, `entry/handler.ts:36-63`, and `CreateTrackerDialog.tsx:443-512` show the shallow config/value/metadata shape. | `Documentation/8_Client_Context.md:141-162` explicitly calls custom trackers poorly designed/not production-ready. `Documentation/Product/PHASE_2_REALITY_AUDIT.md:24` says custom trackers are simple definitions, not the schema/widget/analytics system. | The current system is useful as a simple tracker registry but risky as the foundation for future custom analytics. | Treat custom trackers as a redesign milestone, not incremental polish. |
| Specialized default trackers are not reduced to custom trackers | PARTIAL | Weight has specialized contracts/read models (`Documentation/6_Chimero_Features.md:31`, `:34`); most other defaults are seeded generic rows in `tracking/handler.ts:10-21`. | `Documentation/8_Client_Context.md:149` says specialized trackers should not be reduced to shallow generic entries. | Weight is specialized; most other default trackers are still generic or partial. | Specialize defaults only after each tracker contract proves required fields and read models. |
| Meaningful schema/analytics/correlation support for future trackers | MISSING | No code evidence of custom field schema tables, schema-builder UI, field-level stats, or field-level correlations beyond generic `config`/`metadata`. | `Documentation/8_Client_Context.md:82-86`, `:149-162`, `:842-865` require future tracker contracts with fields, widgets, stats, graphs, entries, tags, assets, and correlations. | This is the core missing system behind the shallow custom tracker concern. | Write a dedicated custom tracker contract/design before implementing any more custom tracker UI. |
