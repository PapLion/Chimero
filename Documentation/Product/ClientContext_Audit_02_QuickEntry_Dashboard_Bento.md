# Client Context Audit 02 - Quick Entry, Dashboard, Bento

Scope: `Documentation/8_Client_Context.md` lines 1-939 only. Compared against `chimero-habit-flow/` and `Documentation/`.

| Requirement | Status | Code Evidence | Documentation Evidence | Gap | Recommended Next Action |
| --- | --- | --- | --- | --- | --- |
| Quick Entry baseline fast daily logging | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/entry/components/QuickEntry.tsx:48` defines the modal; `QuickEntry.tsx:232` handles submission; `Documentation/6_Chimero_Features.md:21` classifies Quick Entry as ACTIVE/PARTIAL. | `Documentation/8_Client_Context.md:16` says Home = quick daily logging; `Documentation/8_Client_Context.md:176` says Quick Entry must avoid repetitive manual input. | The modal exists and saves entries, but strict product intent includes reusable recent/frequent/history item logging beyond choosing a tracker. | Keep existing modal, then add explicit past-entry/history and common-item logging flows before treating Quick Entry as complete. |
| `Alt + Q` opens Quick Entry | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/features/entry/components/QuickEntry.tsx:216` registers the keyboard shortcut; `QuickEntry.tsx:219` checks `e.altKey && e.key === "q"`; `QuickEntry.tsx:221` toggles Quick Entry. | `Documentation/8_Client_Context.md:538` confirms `Alt + Q` as the intended shortcut; `Documentation/8_Client_Context.md:554` says `Alt + Q` should open the compact modal. | No code gap found from static inspection. Manual keyboard testing would still be useful. | Add a lightweight UI/manual test note for `Alt+Q` if this audit later becomes a verification checklist. |
| Visible Quick Entry trigger/button | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/shared/components/FloatingActionButton.tsx:11` renders the fixed `+` button; `FloatingActionButton.tsx:12` toggles Quick Entry; `App.tsx:71` shows the `Alt+Q` hint. | `Documentation/8_Client_Context.md:538` says the trigger should be visible and understandable. | No static code gap found. | Preserve the button and shortcut hint in future layout changes. |
| Recent/frequent/history entries | PARTIAL | `chimero-habit-flow/apps/electron/src/main/features/entry/handler.ts:127` returns quick-entry context; `handler.ts:129` queries recent trackers; `handler.ts:169` returns `recentTrackers`, `favoriteTrackers`, tags, and suggestedTags; `QuickEntry.tsx:551` renders Favorites and `QuickEntry.tsx:555` renders Recent. | `Documentation/8_Client_Context.md:178` asks for past entries, most-used entries, or common items; `Documentation/8_Client_Context.md:355` asks for recent/frequent/history items; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:15` already marks this Partial. | Current implementation has recent/favorite trackers and suggested tags, not frequent entries, history entries, or a true past-entry chooser. | Add a quick-entry history/common-items read model and UI section separate from recent trackers. |
| Side-by-side tracker picker | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/entry/components/QuickEntry.tsx:454` uses an auto-fit grid for tracker buttons; `QuickEntry.tsx:542` still constrains the picker to `max-h-[300px]` with vertical scrolling. | `Documentation/8_Client_Context.md:542` rejects a long vertical list; `Documentation/8_Client_Context.md:544` asks for compact side-by-side recent trackers/items. | Tracker buttons are grid-based, but sections still stack vertically and can scroll; there is no richer side-by-side grouping of recent/frequent/common items. | Redesign the picker into horizontally grouped/columnar sections and verify it avoids unnecessary scrolling at common desktop sizes. |
| Context-aware Quick Entry inside tracker detail pages | DONE | `chimero-habit-flow/apps/electron/src/renderer/src/features/dashboard/components/BentoGrid.tsx:128` renders `TrackerDetailView` when `activeTracker` is set; `QuickEntry.tsx:163` preselects `activeTracker` when opened; `QuickEntry.tsx:440` detects Weight and `QuickEntry.tsx:274` routes Weight through the specialized mutation. | `Documentation/8_Client_Context.md:583` requires tracker-page context awareness; `Documentation/8_Client_Context.md:585` says `+` or `Alt + Q` should default to the current tracker; `Documentation/8_Client_Context.md:587` gives Weight as the direct-add example. | Static code supports the flow. Manual testing should confirm both the `+` button and `Alt+Q` behave the same from every detail route. | Add a focused manual regression check for tracker detail -> `+` and tracker detail -> `Alt+Q`. |
| Enter-to-submit | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/entry/components/QuickEntry.tsx:573` wraps the entry form with `onSubmit`; `QuickEntry.tsx:708` and `QuickEntry.tsx:744` explicitly submit on Enter for text/number inputs; `QuickEntry.tsx:911` renders the submit button. | `Documentation/8_Client_Context.md:580` says Enter should add/submit an entry. | Text and numeric flows are covered, but social/contact bubble, exercise search, rating button, and asset/tag-focused flows need manual keyboard verification. | Add keyboard acceptance coverage for every Quick Entry branch, especially Social, Exercise, Mood/rating, and Reminder mode. |
| Home dashboard glanceability | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/dashboard/components/WidgetCard.tsx:429` renders media assets when entries have `assetId`; `WidgetCard.tsx:854` implements a Social widget; `WidgetCard.tsx:1014` routes widgets by tracker heuristics. | `Documentation/8_Client_Context.md:182` requires visual glanceability; `Documentation/8_Client_Context.md:184` asks for bubbles/initials/images and TV/Gaming squares/icons/assets; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:19` marks Gaming/TV identity Partial. | Glanceable widgets exist, but several surfaces still rely on heuristics and notes; Social widget extracts names from text rather than contact/avatar read models. | Add real contact/avatar and media/game item read models for dashboard cards instead of relying on generic notes and tracker-name heuristics. |
| Bento/custom layout rendering | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/dashboard/components/BentoGrid.tsx:68` defines the grid; `BentoGrid.tsx:140` wraps it in DnD; `WidgetCard.tsx:1024` maps small/medium/large size classes. | `Documentation/8_Client_Context.md:84` asks for movable, configurable cards/widgets; `Documentation/8_Client_Context.md:214` says custom layout was basic while widgets remained partial; `Documentation/8_Client_Context.md:216` says Bento/Grid was demonstrable but not production-polished. | Rendering is present, but this is still a tracker-card dashboard, not a full custom widget/schema/layout engine. | Keep classifying as production-hardening work, not a completed custom layout platform. |
| Drag/drop behavior | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/dashboard/components/BentoGrid.tsx:87` configures pointer/keyboard sensors; `BentoGrid.tsx:98` handles drag end; `BentoGrid.tsx:105` uses `arrayMove`. | `Documentation/8_Client_Context.md:238` lists drag/drop behavior as a review problem; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:25` says drag/drop exists but polish is early. | Drag/reorder exists, but no manual interaction-quality verification was run in this audit. | Manually test drag start thresholds, keyboard sorting, accidental drags, and touch/trackpad behavior. |
| Resize behavior | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/shared/store.ts:10` includes widget `size`; `WidgetCard.tsx:1024` maps size classes; `BentoGrid.tsx:108` persists size values when saving layout. | `Documentation/8_Client_Context.md:241` lists resizing as a review problem; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:25` says resizing remains early. | Size rendering exists, but no visible resize controls or resize interaction were found. | Add explicit resize UI/commands and define size collision rules before marking this Done. |
| Layout persistence | DONE | `chimero-habit-flow/packages/db/src/schema.ts:13` stores `settings.dashboardLayout`; `chimero-habit-flow/apps/electron/src/main/features/tracking/handler.ts:174` reads layout; `handler.ts:187` saves layout; `BentoGrid.tsx:72` consumes saved layout. | `Documentation/8_Client_Context.md:243` lists persistence as required hardening; `Documentation/Contracts/Database_Contracts.md:36` documents dashboard layout JSON persistence. | Static code shows persistence. Manual reload verification was not run. | Add a reload regression check after drag/reorder changes. |
| Collision behavior | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/dashboard/components/BentoGrid.tsx:12` imports `closestCenter`; `BentoGrid.tsx:23` uses `rectSortingStrategy`; `BentoGrid.tsx:140` wires collision detection. | `Documentation/8_Client_Context.md:240` lists collision handling as a problem to review; `Documentation/Product/PHASE_2_REALITY_AUDIT.md:25` keeps this Partial. | There is library-level collision detection, but no custom collision policy, acceptance tests, or manual proof. | Test dense grids and mixed widget sizes; document or implement expected collision/empty-slot behavior. |
| Responsive behavior | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/features/dashboard/components/BentoGrid.tsx:142` uses a 12-column responsive grid; `WidgetCard.tsx:1025` uses responsive col spans; `index.css:44` changes sidebar width at smaller breakpoints. | `Documentation/8_Client_Context.md:50` says old overflow issues made responsiveness important; `Documentation/8_Client_Context.md:242` lists responsive behavior as a Bento issue. | Code has responsive classes, but this audit did not run viewport/browser checks and docs still call responsiveness a hardening area. | Run desktop/mobile viewport checks for overflow, text clipping, and drag behavior before upgrading status. |
| Collapsible sidebar | PARTIAL | `chimero-habit-flow/apps/electron/src/renderer/src/shared/store.ts:34` defines `sidebarCollapsed`; `store.ts:65` defines `toggleSidebar`; `Sidebar.tsx:82` renders a fixed sidebar; `Sidebar.tsx:101` only collapses/expands the Tracking section. | `Documentation/8_Client_Context.md:64` says the sidebar could collapse/expand with a button; `Documentation/8_Client_Context.md:208` refers to collapsible sidebar behavior. | App-level collapsed state exists but is not consumed by `Sidebar`; current code only has responsive label hiding and Tracking dropdown expansion. | Wire the app-level sidebar collapsed state to actual sidebar width/labels and add a visible toggle, or remove the stale state if intentionally abandoned. |
